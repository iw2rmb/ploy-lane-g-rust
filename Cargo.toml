[package]
name = "hello"
version = "0.1.0"
edition = "2021"

[dependencies]
hyper = { version = "0.14", features = ["server", "http1"] }
tokio = { version = "1", features = ["macros", "rt-multi-thread"] }
